spring:
  datasource:
    url: ${DB_URL}
    username: ${DB_USERNAME}
    password: ${DB_PASSWORD}
    hikari:
      maximum-pool-size: 5
      minimum-idle: 1

  jpa:
    show-sql: false

  flyway:
    enabled: true

logging:
  level:
    root: WARN
    com.taskmanagement: INFO
